{"_note":"GENERATED by the evaluation harness's demand-attribution emitter (tools/demand-attribution.py). Do not hand-edit \u2014 re-run the emitter. Extraction only: every classification is made by the pack compiler that reads this file.","_schema":"fuaran.pack.demand-attribution/v1"}
{"cluster": "", "date": "2026-07-18", "disposition": "Shipped — fuaran-core#89 coercion", "families": ["claude-opus"], "intent": "flat filter step `{column,op,param}`", "sourceLine": 31}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — Phase-93-family one-to-one alias candidate (`Search` ⇢ `Text` in the FormFieldKind position; 2 sightings, 1 family, judge 1.000 both — intent fully correct); demand-gate on pilot 5", "families": ["gpt"], "intent": "filter-chip control `$type: \"Search\"` — a search-box filter item; canonical is `Text` (auto-binds the filter + §16 search canonicalisation)", "sourceLine": 36}
{"cluster": "", "date": "2026-07-20", "disposition": "Not demand — the decode didactic already names the authorable actions; Phase 627 repair-turn fodder; watch in pilot 5", "families": ["gemini"], "intent": "`onSubmit: \"<closure>\"` — a placeholder string where a typed Action object belongs", "sourceLine": 37}
{"cluster": "", "date": "2026-07-19", "disposition": "Confirmed demand — emitted the exact Core#91 `in`/`param` wire, one `predicate`⇢`pred` alias away; drives fuaran#600", "families": ["claude-opus"], "intent": "multi-select membership binding", "sourceLine": 40}
{"cluster": "033/c6", "date": "2026-07-20", "disposition": "**Shipped — fuaran#632 (0.2.10, fuaran 7d6f25b, corpus 6e34f04; cat:Fuaran.UI.TransformScalarSlot)**: scalar slots resolve a Transform to the lone cell of an exactly-1×1 result; the terminals are the EXISTING verbs (`project` one col + `limit` 1; `groupBy(keys:[], aggs:[one agg])` — no new vocabulary, no Core change); >1×1 loud didactic, empty ⇒ absence except a trailing global `count` ⇒ 0", "families": ["gpt"], "intent": "**Transform in a SCALAR slot** — derive a single value (row-field lookup, count, aggregate) from embedded data into a text/badge/metric binding; the r42 emission is canonical-modulo-slot (Selection-param → filter → project → Callout body)", "sourceLine": 43}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — lenient-shorthand candidate (the row-major twin of Core#88's columnar shorthands); demand-gate on pilot 5", "families": ["claude-opus"], "intent": "embedded Transform `source` as a ROWS ARRAY (array of objects) rather than columnar", "sourceLine": 46}
{"cluster": "031/c7,034/c5,035/c7,037/c6", "date": "2026-07-20", "disposition": "Not a language gap — teaching (pack \"Filters must be WIRED\" + `filterable-static-dashboard` fixture, fuaran 3f1f2db 2026-07-19); censused cells predate the fix; pilot 5 measures the delta as a named line", "families": ["claude-opus", "gemini", "gpt"], "intent": "filters declared but not WIRED — consumers fed unwired Static instead of `dependsOn`/Transform off the filter", "sourceLine": 48}
{"cluster": "032/c2,032/c6,036/c8", "date": "2026-07-20", "disposition": "~~Same class as 040/c3 — Shipped fuaran#629 `Selection.defaultValue` (0.2.9); censused cells predate the fix; re-check in pilot 5~~ — **rationale FALSIFIED 2026-08-01.** The `grok-full-20260731T072032Z` cells post-date #629 on 0.11.0 and show `defaultValue` **adopted** (\"bound via Selection … with defaultValue UA451\"), yet 032/c6 and 036/c8 both return ×3. The fix worked; a narrower demand remains underneath it — **projecting MULTIPLE fields from one selected row** (one field binds, siblings stay hard-coded). Re-opened; needs falsifier — see `[ref]`", "families": ["claude-opus", "gemini", "gpt"], "intent": "detail panel / callout driven by the grid-selected row with a default selection — the defaulted-Selection master-detail", "sourceLine": 49}
{"cluster": "039/c1,039/c3", "date": "2026-07-20", "disposition": "Not a gap — teaching emphasis, same class as 038/c4; the pre-selected-idioms pack section may lift it; watch in pilot 5", "families": ["claude-opus"], "intent": "filter/selector preselected on load (date-range last-90-days; status Active) — `Filter.defaultValue` exists since 0.2.0", "sourceLine": 50}
{"cluster": "032/c3,033/c4", "date": "2026-07-20", "disposition": "Same class as Transform-in-scalar-slot (the strongest open cluster above) — additional demand evidence for that design phase", "families": ["claude-opus", "gemini", "gpt"], "intent": "scalar derived from embedded data into a badge/text slot (pick-task count badge; ward-occupancy percentage)", "sourceLine": 51}
{"cluster": "", "date": "2026-07-20", "disposition": "Not demand — the 613 NO-GO artifact class (under-informed models invent vocabulary; the full pack teaches the real cases and is load-bearing); excluded from language dispatch; pilot 5 runs full-pack postures only", "families": ["claude-opus", "gemini", "gpt"], "intent": "invented vocabulary under deliberately-slimmed packs: region/Region/group/main/Main/section/Section/Subsection/Title/Heading/Stack/Flow/Page/Display/Compact/DateRange/Enum/None/Toolbar/toolbar/banner/tabpanel/Bound/Subtle/Custom + missing kind/options/label/children/role fields", "sourceLine": 53}
{"cluster": "", "date": "2026-07-20", "disposition": "Not demand — posture/harness class (616 retired the default arms; low cap fixes); truncation is Phase 627 auto-repair fodder", "families": ["claude-opus", "gpt"], "intent": "emission truncation / mid-JSON cutoff (Sonnet default-adaptive-thinking death-spiral + generic truncation)", "sourceLine": 54}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — gate FIRED in pilot-5 n=1 (×5 more) but inspection splits the class: the n=1 instances are TEXT in `Metric.value` (\"Detection (PagerDuty)\") — not coercible; the intent is a text-valued KPI (Fact/LabelValueRow teaching, or vocabulary demand for a text Metric). Reclassify per-instance; numeric-string subclass stays a lenient candidate", "families": ["claude-opus", "gemini", "gpt"], "intent": "string/sentinel in a numeric value slot (the NaN-template decode error) — the min/max string-number lenient candidate from the 2026-07-20 collision sweep", "sourceLine": 55}
{"cluster": "", "date": "2026-07-20", "disposition": "**Shipped — fuaran-core#94 (0.1.10, fuaran 0.2.11)**: all four decode to canonical twins, corpus 235 (`lenient-transform-{values-only-columns,flat-or,epoch-timestamps,flat-scalar-fn}`); free re-gate of stored n=1: +13 cells, 83.6%→87.4% parse", "families": ["claude-opus", "gemini", "gpt"], "intent": "four near-canonical lenient shapes: values-only column object (`{\"values\":…}` without the `validity` mask); flat logical/comparison spellings; epoch ints in declared-timestamp columns (`finish_ts`/`finish`/`timestamp`/`lastRestock` sightings); flat scalar-fn spellings", "sourceLine": 56}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — ambiguous (not coercible under the admission law); didactic candidate: derive-entry reject naming `name`/`expr`; watch in pilot-5 n≥2", "families": ["gemini"], "intent": "derive-entry fields spelled `a`/`b` (+ nested named-arg `dateDiffDays`) — TWO liberties at once, no confident one-to-one mapping", "sourceLine": 57}
{"cluster": "", "date": "2026-07-21", "disposition": "Measured — cross-arm hard-stratum shakedown (r80-82, 150 cells) VERDICT: **graduate PER-FAMILY, not globally**. opus −49% out/quality holds; terra −31%/holds; sol −46%/−5pp (within judge noise, watch); **gemini +19pp parse (48→67%) — the biggest quality win**; **kimi BREAKS: parse 85→50%, 14/15 fails raw JSON syntax breakage from the \"Compact JSON\" line** — frugality is a per-provider posture flag (623-registry style), K3 excluded", "families": ["claude-opus"], "intent": "`fuaran-terse` frugality appendix on opus@low: **−33.5% output tokens (2,134→1,420), parse 18/18 (from 83%), pass 78% (from 67%)** — the padding was CAUSING decode failures", "sourceLine": 58}
{"cluster": "028/c6", "date": "2026-07-20", "disposition": "Open — classify by falsifier (is corner-anchored overlay placement expressible, or tone/teaching?)", "families": ["claude-opus", "gemini", "gpt"], "intent": "toast stack anchored to a viewport corner (top-right) — overlay/viewport positioning intent", "sourceLine": 60}
{"cluster": "043/c3", "date": "2026-07-20", "disposition": "Open — classify by falsifier (teaching vs a switch-affordance gap in the canonical field vocabulary)", "families": ["claude-opus", "gemini", "gpt"], "intent": "a start/stop toggle-switch control affordance (the retired `Toggle` name's intent, judged PARTIAL on the canonical spellings)", "sourceLine": 61}
{"cluster": "044/c6", "date": "2026-07-20", "disposition": "Open — likely expressible since fuaran#632 (Transform params take any Binding incl. State → scalar slot); falsifier: teach the composition, shakedown", "families": ["claude-opus", "gpt"], "intent": "total cost computed live from current form selections — a derived scalar over STATE bindings", "sourceLine": 62}
{"cluster": "049/c4", "date": "2026-07-20", "disposition": "Open — classify by falsifier (Invoke/Query response-destination wiring: teaching vs expressiveness)", "families": ["claude-opus", "gemini", "gpt"], "intent": "refresh action fetches from host endpoint AND declares where the response lands (request list reads that destination) — the declared data-flow round-trip", "sourceLine": 63}
{"cluster": "", "date": "2026-07-20", "disposition": "**Shipped — fuaran#633 (0.2.12, 1a8ee73; corpus 236)**: `Bound` unwraps in bare-Binding slots; free re-gate flipped the claude cells (opus arms 86/85 → 91.1/90.0 fresh parse)", "families": ["claude-opus"], "intent": "`{\"$type\":\"Bound\",\"binding\":X}` in a bare-Binding slot (Metric.value) — the canonical `TextSource.Bound` wrapper convention transferred uniformly to non-TextSource binding slots", "sourceLine": 64}
{"cluster": "023/c1", "date": "2026-07-20", "disposition": "**Open — the strongest new capability demand of pilot 5**; temporal-capability family (pairs with the date-arithmetic row); design question: a host-provided environment binding (`Now`/`Today` via context), never a wire-side clock", "families": ["claude-opus", "gemini"], "intent": "**the CURRENT DATE in a header** — a now()/today value; the wire is deliberately clock-free so the intent is inexpressible (models hardcode a date, judged PARTIAL)", "sourceLine": 65}
{"cluster": "044/c6,061/c3,061/c4,061/c6", "date": "2026-07-20", "disposition": "Open — likely expressible post-#632 (Transform params take any Binding; `concat` exists) — same family as 044/c6 computed-total-over-State; falsifier: teach the composition, shakedown", "families": ["gemini", "gpt"], "intent": "badge preview COMPOSED live from form-field values (name/company/track concat into a text template) — live string composition over Local/State bindings in a text slot", "sourceLine": 66}
{"cluster": "058/c5", "date": "2026-07-20", "disposition": "Not a gap — teaching/emphasis class (apply the format everywhere, not once); watch", "families": ["claude-opus", "gemini", "gpt"], "intent": "consistent currency formatting across money values — `Format.Currency` EXISTS", "sourceLine": 67}
{"cluster": "011/c5", "date": "2026-07-20", "disposition": "Open — classify by falsifier (visibility-state teaching vs a state-machine gap); single-family", "families": ["claude-opus"], "intent": "confirmation framed as a post-submission success STATE (not always-visible chrome) — conditional visibility semantics", "sourceLine": 68}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — lenient-default candidate (infer headers from row shape?) vs didactic; demand-gate next wave", "families": ["gpt"], "intent": "`staticRows` emitted without `headers`", "sourceLine": 69}
{"cluster": "023/c7", "date": "2026-07-20", "disposition": "**Shipped (language arm) — fuaran#637 (ea491c5)**: the row's directed surface check resolved the class structurally — `ChartSpec.Stacked` rode the wire (fuaran#126) but `Charts.lower` ignored it, so the intent was unrenderable regardless of teaching (language layer, no falsifier run needed to establish inexpressibility); stacked Bar + Area/stacked-Area arms now lower first-party, goldens `bar-stacked`/`area-single`/`area-multi`/`area-stacked`; stage-5 verification shakedown (023 ×3 postures; any teaching/rubric residue classifies per stage 3) pending the next operator-run cohort", "families": ["gemini", "gpt"], "intent": "a stacked status-breakdown chart in a side panel — the `charts` vocabulary EXISTS (bar/line lowering) but stacked-series composition may not", "sourceLine": 70}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — inspect the emission shape next wave (likely the flat-comparison family with a positional variant)", "families": ["gemini", "gpt"], "intent": "a pipeline/binary node missing `left` (flat-spelling remnant in a label-binding pipeline)", "sourceLine": 71}
{"cluster": "", "date": "2026-07-20", "disposition": "Open — lenient-default candidates (absent layout ⇒ Auto; absent wrap ⇒ false); n=3 gate read: ZERO recurrence — stays demand-gated (watch in n=6 / pilot 6)", "families": ["gemini"], "intent": "`Box` emitted without `layout` (and a Flex without `wrap`) — MISSING_FIELD `layout`/`wrap`; the language has an `Auto` layout and `wrap` has an obvious false default", "sourceLine": 74}
{"cluster": "", "date": "2026-07-26", "disposition": "**Shipped — Fuaran.UI 0.3.1 (fuaran-dotnet d8b8cda)**: the path resolver canonicalises a leading lower-case character, so the camelCase wire spelling applies and the PascalCase one is unchanged (upper-casing an upper character is the identity). **Language/engine defect, not model error** — the `contains` pattern, caught at intake this time. Cross-family on first sighting ⇒ dispatch-worthy per the census threshold. Guarded by `InputAndWireSpellingTests` (both spellings must yield identical trees; an unknown field must still be `FieldNotFound`) and declared stable surface in `[ref]`, so re-narrowing is a breaking change", "families": ["claude-opus", "gpt"], "intent": "**`UpdateProp.path` resolves PascalCase field names whilst the whole rest of the wire is camelCase.** Both families independently emitted `path:\"subtext\"` against a `Metric`; the probe confirms `path:\"Subtext\"` APPLIES and `path:\"subtext\"` is refused `FieldNotFound`. The emission is canonical in intent and spelled the way every other wire field is spelled", "sourceLine": 76}
{"cluster": "", "date": "2026-07-26", "disposition": "**Shipped — Fuaran.UI 0.3.1 (fuaran-dotnet d8b8cda)**: a leading `kind.` is stripped when a segment follows it. Same root cause as the camelCase row — the engine refusing a wire-literal spelling. Guarded: a bare `kind` path still refuses (that is `EditNode`'s job) and a non-leading `Fields[i].Kind` stays deliberately unaddressable. **Found only because the fix for the first two rows would otherwise have left one family's arm still failing** — a reminder that a per-family census beats a pooled one", "families": ["gpt"], "intent": "**A redundant leading `kind.` segment on `UpdateProp.path` was refused**, although the serialised tree genuinely nests spec fields under a `kind` object (`{\"id\":…,\"kind\":{\"$type\":\"Metric\",\"subtext\":…}}`) — so `kind.subtext` is the path the field VISIBLY occupies, whilst `path` is rooted inside the spec. Two defensible readings of one document; the engine accepted only one", "sourceLine": 78}
{"cluster": "", "date": "2026-07-28", "disposition": "**Verdict recorded (678): genuine assist under 673's criterion** — a §16 name-only normalisation (`ChoiceFilter`⇢`Choice`, `SegmentedFilter`⇢`SegmentedChoice`, `TextFilter`⇢`Text`, `RangeFilter`⇢`Range`; payload validity still decided by the canonical case) is the candidate shape; NOT a kind re-admission. Operator-gated — it re-admits retired names; pack now states the retirement explicitly (fuaran-dotnet e34c449); re-measure incidence post-719 before deciding", "families": ["claude-opus", "gemini", "gpt"], "intent": "retired 0.1.x `-Filter` spellings (`ChoiceFilter` ×194, `SegmentedFilter` ×176, `TextFilter` ×77, `RangeFilter` ×64) emitted while taught the canonical names", "sourceLine": 79}
{"cluster": "", "date": "2026-07-28", "disposition": "**Shipped — fuaran#725 (0.7.0, fuaran-dotnet 5809823 + corpus 6c27f56)**: `FormFieldKind.DateRange` with the bare `{from,to}` canonical pair, lenient array/envelope shapes, from≤to didactic reject, filter auto-binding; host adoption fuaran#730–733; incidence re-measure rides the publication cohort (fuaran#729)", "families": ["claude-opus", "gemini"], "intent": "a single-control date-range field — `Range` is numeric by construction (`Binding<float*float>`), `Date` is single-valued, so the intent is inexpressible as one control", "sourceLine": 81}
{"cluster": "063/c7,064/c9,065/c8", "date": "2026-07-30", "disposition": "**Shipped — fuaran#750 (`Fuaran.UI` 0.11.0)**: `CellKindErased.TonedPill` (`field` + value→`ToneVariant` `map` + omit-when-default `default`) — fuaran-dotnet `cd76b3e`, fuaran-core `d8f2697` (IDL), corpus `0ef4d85`, fuaran-ts `5d5e697`. **No shakedown was needed and none was run** — stage 3's teach-then-shakedown falsifier answers \"is this taught or inexpressible\", and the wire surface answered it by inspection: `Pill`'s tone is a `(row) -> ToneVariant` closure encoding as `\"<closure>\"`, so no teaching and no lenient shape could ever have conjured it. The correct classification for \"the language cannot say this at all\" is **language, immediately** — a shakedown would have spent ~$0.50 confirming a fact readable in the encoder. Post-ship verification per stage 5 rides the next cohort: the pack change supersedes the 729 baseline by definition (the 728-style freeze), so the three canaries' criteria flipping off PARTIAL is the measurement", "families": ["claude-opus", "gemini", "gpt", "grok"], "intent": "**conditional row-level visual distinction by field value in a data grid** — \"delayed / poor-classification / degraded rows visually distinguished (tone, pill, emphasis)\"; one intent, three wordings", "sourceLine": 82}
{"cluster": "028/c2", "date": "2026-07-30", "disposition": "Open — classify by falsifier; pairs with 023/c1 (the pilot-5 \"strongest new capability demand\"); a static tree cannot compute \"ago\" without a temporal binding", "families": ["claude-opus"], "intent": "relative-time rendering (\"N min ago\") — temporal-capability family, same design question as the 023/c1 row (host-provided `Now`/`Today` environment binding, never a wire-side clock)", "sourceLine": 83}
{"cluster": "013/c5", "date": "2026-07-30", "disposition": "Open — log + watch (single sighting)", "families": ["gemini"], "intent": "tone/teaching, not vocabulary", "sourceLine": 84}
{"cluster": "022/c1", "date": "2026-07-30", "disposition": "Open — log + watch (single sighting)", "families": ["gpt"], "intent": "content-fidelity slip, not vocabulary", "sourceLine": 85}
{"cluster": "017/c2,043/c3", "date": "2026-08-01", "disposition": "**Confirmed demand — language layer.** No falsifier needed: expressibility settles it (the `contains`/Core#90 fingerprint). Open design call: new `NodeKind.Toggle` vs a `variant` on the Form checkbox field — vocabulary charter", "families": ["grok"], "intent": "a **boolean toggle/switch input control** — `NodeKind.Switch` is the state-bound conditional (Phase 392), NOT a widget; no toggle input exists in the closed vocabulary", "sourceLine": 87}
{"cluster": "043/c2", "date": "2026-08-01", "disposition": "Open — **pack layer**; fixture-first teaching then shakedown (expect flip)", "families": ["grok"], "intent": "`NodeKind.Progress` EXISTS but is taught in exactly 1 pack file", "sourceLine": 88}
{"cluster": "042/c3", "date": "2026-08-01", "disposition": "Open — **pack layer, emphasis** (same class as 058/c5, 039/c1); shakedown to confirm", "families": ["grok"], "intent": "`Badge`/`TonedPill` exist AND are taught (3 pack files) — not reached for in the chip-vs-tile role", "sourceLine": 89}
{"cluster": "005/c4", "date": "2026-08-01", "disposition": "Split — teach `stacked:true`; **horizontal/funnel logged as a language demand**, ×2, watch", "families": ["grok"], "intent": "`ChartSpec.Stacked` exists (teachable half), but `ChartKind` is closed at Line/Bar/Area/Pie/Scatter/Heatmap — **no Funnel, no horizontal orientation**", "sourceLine": 90}
{"cluster": "032/c6,036/c8", "date": "2026-08-01", "disposition": "**RESOLVED 2026-08-01 — PACK layer, £0.** Falsifier run as an expressibility check: `Binding.Selection(nodeId, accessor, defaultValue, field)` **carries `field`** (shipped fuaran#632), so N sibling `Bound` nodes each projecting a different column IS expressible. The family has been expressible all along — models learned `defaultValue` (#629) and never learned `field`. Dispatch: one fixture-first pack teaching. Folds 054/c5 ×1, 039/c8 ×1, 017/c3 ×1", "families": ["grok"], "intent": "those rows said \"censused cells predate the fix (fuaran#629 `Selection.defaultValue`)\" — **falsified**: these cells post-date it on 0.11.0 and the evidence shows `defaultValue` IS adopted. Residual demand is narrower: **projecting MULTIPLE fields from one selected row** (one field binds, siblings stay hard-coded)", "sourceLine": 91}
{"cluster": "020/c4", "date": "2026-08-01", "disposition": "**Confirmed demand — language layer** (expressibility check, £0). Either `Callout` gains an action slot or an `EmptyState` kind does", "families": ["grok"], "intent": "**a Callout/EmptyState that can hold an action.** `CalloutSpec` is `{ Body; Dismissable; Tone; Heading; Icon }` — **no children or action slot**, so the emission was the best expressible shape", "sourceLine": 92}
{"cluster": "052/c6", "date": "2026-08-01", "disposition": "**RESOLVED — RUBRIC layer, £0.** The emission is canonical and the criterion demands a shape the language deliberately lacks. Correct the criterion to name the id-convention, then `rejudge --since 20260731T072032Z` re-scores stored emissions free", "families": ["grok"], "intent": "none — `FormField` is `{ id; kind; label; required; help }` and the **`id` IS the binding**; there is no explicit-binding slot by design", "sourceLine": 93}
{"cluster": "056/c5", "date": "2026-08-01", "disposition": "**RESOLVED — PACK layer, £0.** Expressible and shipped; taught for the canary tasks but not generalised to severity columns. Same emphasis class as 042/c3", "families": ["grok"], "intent": "column-level tone/variant — `ColumnErased.Kind` is a `CellKindErased`, which is where the Phase-750 `TonedPill` lives", "sourceLine": 94}
{"cluster": "032/c6", "date": "2026-08-02", "disposition": "**Confirmed demand — LANGUAGE layer** (expressibility check, £0). The models emit exactly the right intent against a dead end. Shape options for the charter: `Switch.on` accepting a `Binding`, or a `Computed`/`Selection`-sourced state seed. Sibling of the `toggle-switch-input` demand filed 2026-08-01", "families": ["claude-opus", "grok"], "intent": "**conditional rendering driven by a selection or any binding.** `SwitchSpec` is `{ cases; default; stateKey }` and `stateKey` encodes as a **plain string** (`JStr s.StateKey`) — it names a StateStore key and CANNOT take a `Binding`. The only declarative writer of state is `Action.SetState`, whose `value` is a literal JVal, and a grid's `onRowClick` is a host **closure** (`JStr \"<closure>\"`, not emittable). So \"render this branch based on the selected row\" has **no declarative path at all**; every emission wires a `Switch` to a `stateKey` nothing can write", "sourceLine": 101}
{"cluster": "001/c5", "date": "2026-08-09", "disposition": "Open — classify by falsifier (completeness-class per the flip-5 lesson: an example may not fix \"kept the old layout\"; a repair turn or validator finding may be the lever). Note the task is the pre-v3 `tier-b-001` scaffold — re-measure on the v3 tasks before spending", "families": ["claude-opus", "gemini"], "intent": "**layout reorganization under refinement** — turn 3 asks Revenue alone left / Customers+Sparkline right; models keep the symmetric two-column grid. Expressible (nested Boxes / weighted split); the miss is executing a REORG refinement rather than an ADD", "sourceLine": 107}
{"cluster": "007/c1", "date": "2026-08-09", "disposition": "Open — **pack layer** candidate (kind-choice-adjacent: the default-value slot exists and goes unused); shakedown to confirm", "families": ["claude-opus"], "intent": "**prompt-specified pre-filled value not seeded** — `value: {\"$type\":\"State\",\"key\":…,\"defaultValue\":\"T-Rex Skull\"}` is canonical and taught nowhere as the \"pre-filled with X\" spelling", "sourceLine": 108}
{"cluster": "", "date": "2026-08-09", "disposition": "**Shipped — FLIPPED (pack a89e84c, shakedown `[ref]`)**: the anti-pattern line (\"Badge is a node kind and never a cell kind\") extinguished the cluster — zero recurrences, the worst task 0/3 → 2/2. ~~Census-instrument nit stands: the parse-cluster key (code+token) cannot distinguish wire positions~~ — **nit CLOSED 2026-08-09**: the key gained the normalised wire position (`ParseCluster` slot — `columns[].kind.$type` vs `children[].kind.$type`), labels/drafted rows carry it, and a position-naming log row (like this one) outranks and excludes a token-only collision", "families": ["gpt"], "intent": "**a badge-like cell inside a DataGrid column** — exactly the intent `CellKindErased.TonedPill` (fuaran#750) shipped for; gpt reaches for the node-kind spelling in the cell slot", "sourceLine": 109}
{"cluster": "", "date": "2026-08-09", "disposition": "**POST-EMIT CLOSED, PRE-EMIT a confirmed gpt-4o compliance limit — teaching-proof at BOTH treatments (2026-08-10)**. Post-emit: decoder 0.15.1's cause-naming didactic + one repair round converts **3/3 lifetime** (computed quotients 0.9889 / 0.988 / 0.98888…, judge 1.000 every time) — the production 627 rung is the deployment answer. Pre-emit: the prose rule (a89e84c) AND the exact wrong-then-right pair (602ada7 — the identical `178 / 180` emission shown as failing, in-prompt) both failed to flip: 7/9 base cells across 4 windows emit it verbatim while computing `178` correctly for the sibling Metric. No further teaching attempts — the escalation ladder is exhausted; see the ratio-literal intake row below for the language-side question. **Generation-scoped 2026-08-11:** gpt-5.6-sol@low probe (same tasks, same pack) 4/4 clean — zero arithmetic, full parse+judge; the limit is a gpt-4o LEGACY behaviour, not an OpenAI-family one — terra@low also probed 4/4 clean (2026-08-11), so BOTH current tiers are unaffected. `[ref]`", "families": ["gpt"], "intent": "**inline arithmetic in JSON values** — the model computes a fraction and writes the division; JSON carries numbers only", "sourceLine": 110}
{"cluster": "", "date": "2026-08-09", "disposition": "**RESOLVED — both paths (2026-08-09)**. Post-emit: 796 session-aware context + 797 `--rounds 2` went 2/2 repaired-to-pass. Pre-emit: the live instances showed the trap is FORMATTED QUANTITIES (\"1h 20m\"), not status words — the example treatment (pack `e4c3a2a`, the exact wrong emission beside its `Fact` routing) **extinguished the cluster**: 0/4 canonical pre-treatment → 2/2 post, same task/intent/slot. NOT a compliance limit at any layer. See `[ref]`", "families": ["gpt"], "intent": "Metric-vs-Fact on the TEXT side — the decode didactic exists but only fires AFTER emission; gpt never adopts the Fact routing pre-emit", "sourceLine": 111}
{"cluster": "002/c5", "date": "2026-08-09", "disposition": "**RESOLVED — RUBRIC layer, £0** (the 052/c6 class, with an irony: the criterion demanded the exact wrong spelling the pack now warns against). Criterion corrected to name the badge-like chip class rather than the Badge kind; re-verdict rides the next cohort (MUST-level, so affected cells' pass/fail may flip then)", "families": ["claude-opus", "gpt"], "intent": "none — the emissions are CANONICAL (`TonedPill` is the cell vocabulary; `Badge` is a node kind that does not decode in `columns[].kind` — the very confusion the same-day pack teaching extinguished on the emit side)", "sourceLine": 112}
{"cluster": "", "date": "2026-08-09", "disposition": "Open — first demand-gate read: the `Fact` routing is taught and adopted (2/2), so the ergonomic pressure is relieved; the demand stands as vocabulary evidence (a duration is chartable — the \"would you trend it?\" rule says Metric, which the format vocabulary cannot honour). Re-gate on the next cohort's census", "families": ["gpt"], "intent": "**`CellFormat.Duration`** — the intent is a trendable duration metric (raw seconds/minutes + a duration format), but the vocabulary has Currency/Percent/Number/SignificantDigits/Date only, so models format by hand and trip the numeric-only gate", "sourceLine": 113}
{"cluster": "", "date": "2026-08-10", "disposition": "Taught — pack `ade366b` (one line in the layout section); flip 2/2 clean, zero recurrence. Alias-candidate watch only if it recurs cross-family (`Vertical`/`Horizontal` ⇢ `Flex` shorthand would be a Phase-93-family lenient case)", "families": ["gpt"], "intent": "`layout: {\"$type\":\"Vertical\"}` — direction as the layout discriminator (the same emission used canonical `Flex`/`direction` elsewhere)", "sourceLine": 115}
{"cluster": "", "date": "2026-08-10", "disposition": "**Extinguished — teaching (pack `ade366b`)**: the closure-cells subsection names the discard semantics + the wrong-then-right pair; flip 2/2 canonical Button labels, zero `*Fn`/`lit`/`col`/`Str` post", "families": ["gpt"], "intent": "none — `Button(label, onClick)` takes a static `label`; the confusion is REAL vocabulary inconsistency (`Link`/`Pill`/`Progress` closure slots are `*Fn`-named and wire-inert)", "sourceLine": 116}
{"cluster": "", "date": "2026-08-10", "disposition": "Open — lenient-shorthand-class candidate (the Core#88 / fuaran#633 `Bound`-unwrap precedent: accommodate a universal prior as typed data). Strong gate evidence already (7 sightings, teaching-proof); demand-gate on the next cohort census; the post-emit repair rung keeps it non-urgent", "families": ["gpt"], "intent": "**declarative ratio literal** — the model's prior is that \"178 of 180\" is WRITABLE as a ratio; the language offers only the computed float. Design question (NOT expression evaluation, which stays rejected): a typed static ratio form in fraction slots — e.g. `{\"$type\":\"Static\",\"value\":{\"of\":178,\"outOf\":180}}` — decodable, lossless (the display could even show 178/180), and matching the persistent model prior", "sourceLine": 117}
{"cluster": "007/c2", "date": "2026-08-10", "disposition": "**RESOLVED — rubric corrected in-file (kind-class-neutral)**; SHOULD-level so no pass/fail flips; re-verdict rides the next cohort", "families": ["gpt", "grok"], "intent": "none — RUBRIC layer: the drafted criterion suggested specific kinds (\"such as Metric and Chart\") against the judge's own never-demand-a-specific-kind rule", "sourceLine": 120}
{"cluster": "007/c1", "date": "2026-08-11", "disposition": "Open — teaching-emphasis candidate (pre-selected-idioms pack section); NOTE these sightings surfaced masked under the tier-C batch row via the task-short collision (tier-b-007 vs tier-c-007 both \"007\") — tier-qualified census keys are the recorded 807 hardening follow-up", "families": ["claude-opus", "gpt", "grok"], "intent": "none — the default-value slot EXISTS; models omit the prompt-named default (the 038/c4 teaching-emphasis family, now cross-family)", "sourceLine": 121}
{"cluster": "006/c1", "date": "2026-08-11", "disposition": "Open — watch; classify pack-vs-rubric on the next window (the criterion may under-specify \"unified\")", "families": ["claude-opus", "gpt"], "intent": "none — composition judgment (grouping intent)", "sourceLine": 122}
{"cluster": "004/c3", "date": "2026-08-11", "disposition": "Not demand — single sighting, phrasing-level; watch", "families": ["gpt"], "intent": "none — judge phrasing nuance", "sourceLine": 123}
{"cluster": "tier-c2-009/c3", "date": "2026-08-12", "disposition": "Not demand — the 042/c3 Badge-vs-Fact when-to-use family, single-family, SHOULD-level. Watch; if it recurs cross-family the existing pack Badge example is the lever, not vocabulary", "families": ["gemini"], "intent": "none — `Badge` exists, decodes, and gemini emits it on sibling tasks (tier-c2-006 passes with badges); the miss is a kind-choice preference on a SHOULD criterion", "sourceLine": 124}
{"cluster": "", "date": "2026-08-13", "disposition": "Open — **gate fired; position-qualified row (outranks the token-collision match to the `project.cols` alias row).** Two remedy shapes for the charter: the Core#88-family row-major lenient shorthand (mechanical), and the State-sourced-Transform question (design — sibling of the 032/c6 Switch/binding demand: declarative derivations over mutable state). **Actioned SAME-DAY under the fix-first policy (operator directive 2026-08-13: a cohort halts on a language-defect finding — fix, then re-run fresh; the old epoch's cells are superseded, never published).** Both remedies shipped as fuaran#815 / 0.20.0 (fuaran-dotnet 49cf527): host-bridge normalisation — State/Static/Bound wrapper unwraps (initial-snapshot; the LIVE state-sourced Transform stays the 032/c6 charter), row-major transposes to columnar. The row-major gate row (2026-07-20, two above) closes with it. Flip probe + the fresh Tier-D cohort ride decoder 0.20.0", "families": ["claude-opus", "gemini", "grok", "kimi"], "intent": "**Transform over a State-carried ROW-MAJOR collection** — two entangled demands: (a) the row-major source shape (the 2026-07-20 \"rows array rather than columnar\" row two above — its demand gate has now FIRED, ×3 cross-family, organically at depth); (b) `State` as a Transform source so the count is LIVE over mutable data — the models' intent is exactly right (a badge count that tracks state), and the wire's columnar inline-table source cannot express it", "sourceLine": 125}
{"cluster": "", "date": "2026-08-13", "disposition": "**Flips VERIFIED cross-family:** Duration (stress-009 pass ×2 — the 2026-08-09 row closes as Shipped-verified), Icon (010 ×2 — the 767 sub-finding row closes), form-capture (011 ×2 — the 052/c6 residual closes as Shipped-verified), ratio (012 pass ×2 — the 809 negative holds organically on current models, no arithmetic emitted). **GAPs CONFIRMED firing cross-family:** validation rules (004 fail ×2), drag-reorder (005 ×2), pagination (006 ×2), editable-commit (008 ×2) — all now carry active cross-family sighting evidence, first-in-queue for chartering. **818-family mixed:** live count (001) claude passes via the canonical Transform, terra partial; conditional section (002) passes ×2 (the stateKey degenerate form suffices when the preference IS a state key); row-click→state (003) claude passes, terra fails — the 818 charter's evidence sharpened, not changed. Bound-grid sort (007): claude near-pass (SHOULD partial), terra fails — consistent with the census's \"orderBy near-canonical\" note. Two terra parse fails (005 staticRows cell slip, 006 JSON syntax) are single-family emission errors, not new classes", "families": ["claude-opus", "gpt"], "intent": "active reads on every open census row", "sourceLine": 134}
{"cluster": "stress-001/c2", "date": "2026-08-15", "disposition": "Open — census-intent addition (the 818 derivation family's data-sharing satellite; a named/shared embedded source or tree-scoped data declaration is the shape question). Distinct from the pack: the 834 restore recovered stress-003; this criterion reads the same on pre- and post-slim packs", "families": ["claude-opus", "gpt"], "intent": "**shared data source between sibling nodes** — two nodes cannot reference ONE embedded table; each embeds a copy (`DataSource.Ref` names HOST data only). The models' derivation idiom is right; the wire forces the duplication the judge correctly flags", "sourceLine": 135}
