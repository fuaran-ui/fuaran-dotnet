# Auto-generated F# — do not reformat it. Since Phase 1181 it is emitted IN THIS
# REPO, from src/Fuaran.UI.Idl/{idl,support}.json through the packaged IDL engine,
# and src/Fuaran.UI.Idl.Tests byte-compares the committed file against a fresh
# emission on every `-- Check` and on every push. A Fantomas pass here would break
# that guard, exactly as it would have broken the byte-copy guard this replaced.
#
# To change this file: change the vocabulary in src/Fuaran.UI.Idl/, then
# `FUARAN_REGEN=1 dotnet run --project src/Fuaran.UI.Idl.Tests`.
src/Fuaran.UI/Generated.fs

# Build output — never source. Some projects copy `.fs` sources into their own
# output directory as test data (`src/Fuaran.UI.Tests/bin/**/renderer-sources/`),
# so a build made BEFORE a Fantomas pass leaves pre-format COPIES there and the
# gate's `fantomas --check .` fails naming files nobody edited and git does not
# track. The format check covers tracked sources only; these paths are gitignored
# build artefacts and are excluded here so the stage cannot be reddened by the
# order in which a session happened to build and format.
**/bin/**
**/obj/**

# The F* proof toolkit cache (Phase 1812). `proofs/check.ps1` downloads the F*
# distribution into `proofs/.fstar/` — an untracked, regenerable, per-invocation
# cache full of OCaml `.ml` / `.mli` sources that Fantomas cannot parse. It is
# not source, and a gate run while the cache is present failed the Format stage
# naming files nobody edited (the 1810 and 1812 workers both paid it). Excluded
# here for the same reason `**/bin/**` is: the format check covers tracked
# sources only.
proofs/.fstar/
